<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1" />
  <title>Edit Post - Social Networking Site</title>
</head>
<body>
  <h1>Edit Post</h1>
  <form action="/posts/<%= post._id %>/edit" method="POST">
    <input type="text" name="title" value="<%= post.title %>" required /><br><br>
    <textarea name="content" rows="5" cols="30" required><%= post.content %></textarea><br><br>
    <button type="submit">Update</button>
  </form>
  <br>
  <a href="/posts/<%= post._id %>">← Cancel</a>
</body>
</html>
